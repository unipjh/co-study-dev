# 테스트 결과 주요 변경사항 보고서

**프로젝트**: Co-Study Dev  
**비교 범위**: 1차 테스트 완료 → 실사용자 피드백 반영 최종본  
**작성 기준**: 실사용자 피드백 수렴 후 반영된 변경사항 (2026-06-02)

---

## 테스트 피드백 반영 결과 요약표

> 1차 프로토타입을 실사용자에게 테스트한 뒤 수집된 피드백을 바탕으로 개선된 10가지 항목입니다.

| # | 기능 영역 | 테스트에서 발견된 문제 | 반영된 개선사항 | 개선 효과 |
|---|---|---|---|---|
| 1 | 퀴즈 생성 | 같은 자료로 퀴즈를 여러 번 만들면 비슷한 문제가 반복되어 학습 효과가 떨어짐 | 이전에 생성한 퀴즈를 기억해 유사 문항을 자동으로 걸러내고, 새로운 문항만 출제 | 반복 학습 시에도 다양한 시각의 문제를 통해 학습 효과 유지 |
| 2 | 독해 이해 확인 | 내용을 제대로 이해하지 않아도 다음 페이지로 그냥 넘어갈 수 있어 학습 깊이가 얕아짐 | 학습 단원 끝 페이지에서 O/X 이해 확인 팝업 자동 표시, 틀리면 AI에게 바로 설명 요청 가능 | 수동적 읽기에서 능동적 이해 확인 흐름으로 전환 |
| 3 | AI 채팅 신뢰도 | AI 답변이 자료에 근거한 것인지, 임의로 만들어낸 것인지 구분이 안 됨 | 모든 AI 답변에 근거 신뢰도(충분·부분·약함·없음) 배지 표시, 어느 페이지가 근거인지 확인 가능 | AI 답변을 비판적으로 수용할 수 있어 학습 신뢰도 향상 |
| 4 | 하이라이트 & 메모 | 색상을 잘못 클릭하면 즉시 저장되어 실수를 되돌릴 수 없음 | 색상 선택 후 "메모 쓰기" 또는 "바로 저장" 중 선택, 저장 전 취소 버튼 추가 | 하이라이트 실수 방지, 사용자가 여유 있게 탐색 가능 |
| 5 | 퀴즈 복습 | 이전에 풀었던 퀴즈를 다시 볼 수 없고 틀린 문제만 모아보는 기능이 없음 | 전체 퀴즈 이력에서 "틀린 문제 / 맞힌 문제" 필터로 오답 노트처럼 복습 가능 | 누적 학습 데이터 기반으로 취약 부분 집중 복습 가능 |
| 6 | 마인드맵 | 노드가 많아지면 세로로 길어져 한눈에 파악하기 어렵고, 가끔 화면이 깨짐 | 마인드맵을 가로 방향으로 전환, AI 출력 오류를 자동 보정해 화면 깨짐 방지 | 복잡한 개념 구조도 한눈에 파악 가능, 안정성 향상 |
| 7 | 자료 관리 | 자료가 많아질수록 찾기 어렵고 검색 기능이 없어 불편함 | 좌측 고정 사이드바 + 실시간 검색 바 추가, 폴더별 파일 수 표시 | 자료 수가 늘어도 빠르게 탐색 가능 |
| 8 | 모바일 & 학습 설정 | 모바일에서 상단 버튼들이 겹쳐 보이고, 이해 확인 팝업을 끄는 방법이 없음 | 모바일 화면에 맞게 레이아웃 자동 재배치, 팝업 질문 ON/OFF 토글 스위치 추가 | 모바일 사용성 확보, 학습자가 학습 모드를 스스로 제어 가능 |
| 9 | AI 채팅 입력창 | 자료 맥락을 여러 개 추가하면 채팅 영역이 좁아져 대화가 불편함 | 맥락 표시를 1줄 요약 + 전체 삭제 버튼으로 단순화하여 채팅 공간 확보 | 채팅 흐름을 방해받지 않고 대화 가능 |
| 10 | AI 응답 품질 | AI가 자료와 무관한 내용을 마치 자료에 있는 것처럼 답변하는 경우가 있음 | 자료를 더 세밀하게 분석하는 검색 구조 도입, 근거 없는 페이지 인용 자동 제거 | AI 답변이 실제 학습 자료에 더 정확하게 근거, 오정보 감소 |

---

## 주요 변경사항 요약 (기술 상세)

| # | 변경 영역 | 1차 구현 (테스트 전) | 2차 반영 (피드백 후) | 핵심 개선 효과 |
|---|---|---|---|---|
| 1 | **퀴즈 중복 방지 시스템** | 같은 페이지에서 퀴즈를 반복 생성하면 유사한 문항이 반복 출제됨 | `quizDiversity.js` 도입 — 과거 8세션 이력을 참고해 Jaccard 유사도 0.68 이상 문항 자동 제거, 배제 시 안내 배너 표시 | 반복 학습 시에도 새로운 관점의 문항이 출제되어 학습 효과 유지 |
| 2 | **독해 중 O/X 팝업 게이트** | 학습 단원 이동 시 이해 확인 없이 바로 페이지 전환 가능 | `LearningQuestionPopup` 신규 컴포넌트 — 단원 마지막 페이지 도달 시 자동으로 O/X 이해 확인 팝업 표시, 오답 시 AI 채팅 연동 설명 요청 가능 | 수동적 읽기에서 능동적 이해 확인 흐름으로 전환 |
| 3 | **AI 답변 근거 신뢰도 배지** | AI 채팅 답변에 어느 페이지 내용을 근거로 했는지 알 수 없음 | `contextPipeline.js`의 `classifyEvidenceStatus()` — grounded / partial / weak / none 4단계 배지를 채팅 답변마다 표시, 출처 페이지 목록 툴팁 제공 | AI 답변의 신뢰도를 사용자가 직접 판단 가능 |
| 4 | **하이라이트 색상 선택 UX 개선** | 색상 클릭 즉시 메모 입력 단계로 강제 전환되어 실수 취소 불가 | `SelectionToolbar` 재설계 — 색상 선택 후 "메모 쓰기" / "바로 저장" 2단계 선택지 제공, `AnnotationPopup`에 변경 사항 취소 버튼 추가 | 의도치 않은 색상 저장 실수 방지, 탐색적 색상 선택 가능 |
| 5 | **오답 노트 & 퀴즈 세션 회고** | 현재 세션 문항만 표시, 과거 세션 복기 불가 | `QuizPanel`에 "틀린 문제 / 맞힌 문제" 필터 칩 추가 — 전체 세션 이력에서 정오답 구분 조회, O/X 팝업 정답 기록도 동일 화면에 통합 표시 | 누적 학습 이력 기반 취약 부분 집중 복습 가능 |
| 6 | **마인드맵 레이아웃 & 안정성** | Top→Bottom 세로 트리 구조로 노드가 많을 때 가독성 저하, AI 출력 오류 시 렌더링 크래시 | `rankdir: 'LR'` 가로 트리 전환 + `mindmapValidation.js` 도입 — 잘못된 AI 출력(순환 구조, 누락 레벨 등) 자동 정규화, 브랜드 컬러 기반 노드 팔레트 적용 | 복잡한 개념도도 한눈에 파악, AI 오류로 인한 화면 깨짐 방지 |
| 7 | **문서 라이브러리 UI 전면 재설계** | 단순 리스트 + 상단 헤더 구조, 검색 기능 없음 | 좌측 고정 사이드바(408px) 레이아웃으로 전환 + 실시간 검색 바 추가 + 폴더별 파일 수 표시 + 정렬 드롭다운으로 통합 | 자료가 많아질수록 탐색 효율 향상 |
| 8 | **툴바 반응형 + 팝업 질문 토글** | 모바일에서 툴바 요소가 겹치고, 학습 게이트 ON/OFF 제어 없음 | `TopToolbar` 760px 이하에서 컴팩트 단일 컬럼 레이아웃 전환 + "팝업 질문 받기" iOS 스타일 토글 스위치 추가 | 모바일 사용성 확보, 사용자가 학습 모드 직접 제어 가능 |
| 9 | **채팅 맥락 표시 방식 간소화** | 맥락 추가 시 상단에 개별 칩이 나열되어 채팅 영역 잠식 | 입력창 위 단일 미리보기 영역으로 통합 — 맥락 요약 제목 + 본문 1줄 + 전체 삭제 버튼만 표시 | 채팅 영역 확보, 맥락 관리 UI 단순화 |
| 10 | **RAG 컨텍스트 파이프라인 체계화** | AI에게 전달되는 문서 맥락이 페이지 단위로 비효율적으로 구성됨 | `chunking.js`(단락 단위 900자 청크) + `contextPipeline.js`(의미 검색 + 선택 앵커 + 현재 페이지 3소스 병합) 도입, 잘못된 페이지 인용 자동 제거 | AI 답변의 근거 정확도 향상, 페이지 번호 환각 감소 |

---

## 변경사항 상세

### 1. 퀴즈 중복 방지 시스템

**관련 파일**: `src/lib/quizDiversity.js`, `src/components/Sidebar/QuizPanel.jsx`

- 퀴즈 생성 시 최근 8개 세션 이력을 `buildQuizHistory()`로 수집
- `filterNovelQuizItems()`가 character-shingle Jaccard 유사도 0.68 이상인 문항 자동 제거
- 제거된 문항이 있을 경우 "이전 퀴즈와 유사한 후보 문항을 제외하고 새 문항으로 구성했습니다." 배너 표시
- "전체 페이지" 범위 신규 추가 — 문서 전체를 8개 페이지 대역으로 분산하여 매 생성마다 다른 영역을 커버

---

### 2. 독해 중 O/X 팝업 게이트

**관련 파일**: `src/components/Canvas/LearningQuestionPopup.jsx` (신규), `src/components/Canvas/DocumentCanvas.jsx`, `src/lib/learningQuestionGate.js` (신규)

- 학습 단원의 마지막 페이지 도달 시 O/X 이해 확인 팝업 자동 표시
- `mode='blocked'`: 답변 전 페이지 이동 차단 / `mode='review'`: 비차단 인라인 피드백
- 오답 또는 "더 쉽게 설명" 클릭 시 AI 채팅 패널에 해당 질문 자동 전송
- "팝업 질문 끄기" 버튼으로 기능 영구 비활성화 가능
- 팝업 O/X 답변 기록은 `QuizPanel`의 "저장된 퀴즈" 탭에 통합 표시

---

### 3. AI 답변 근거 신뢰도 배지

**관련 파일**: `src/lib/ai/contextPipeline.js` (신규), `src/components/Sidebar/ChatPanel.jsx`

| 배지 | 의미 |
|---|---|
| `grounded` | 문서 내 명확한 근거 존재 |
| `partial` | 일부 근거 존재 |
| `weak` | 근거가 희박함 |
| `none` | 문서에서 확인 불가 |

- `classifyEvidenceStatus()`가 AI 응답 분석 후 배지 상태 결정
- 배지 툴팁에서 근거로 사용된 페이지 목록 확인 가능
- 잘못된 `[p.N]` 인용은 `stripInvalidCitations()`로 자동 제거

---

### 4. 하이라이트 색상 선택 UX 개선

**관련 파일**: `src/components/Canvas/SelectionToolbar.jsx`, `src/components/Canvas/AnnotationPopup.jsx`

- **기존**: 색상 클릭 → 즉시 메모 입력 단계로 이동 (취소 불가)
- **변경**: 색상 클릭 → "메모 쓰기" 또는 "바로 저장" 선택지 제공
- `AnnotationPopup`에 `hasDraftChanges` 상태 도입 — 변경 내용 미저장 시 취소/저장 버튼 표시
- 커스텀 색상 선택기가 숨겨진 `<input type="color">` 방식에서 인라인 색상 휠 + HEX 직접 입력 패널로 교체
- 툴바 테마 다크(#1a1a1a) → 라이트(#ffffff)로 전환, 앱 전체 디자인 언어와 통일

---

### 5. 오답 노트 & 퀴즈 세션 회고

**관련 파일**: `src/components/Sidebar/QuizPanel.jsx`, `src/hooks/useQuizSessions.js` (신규)

- "틀린 문제" / "맞힌 문제" 필터 칩 추가 — 전체 저장 세션 대상으로 필터링
- 필터 활성화 시 모든 `QuizCard`가 `readOnly=true`로 전환, 정답(초록)/오답(빨강) 하이라이트 표시
- "이 퀴즈 열기" 버튼으로 해당 세션의 편집 모드로 즉시 이동 가능
- O/X 팝업 답변 기록(`popup_ox`)이 동일 탭에 "팝업O/X퀴즈" 칩으로 통합 표시

---

### 6. 마인드맵 레이아웃 & 안정성

**관련 파일**: `src/components/MindMap/MindMapCanvas.jsx`, `src/components/MindMap/MindMapNode.jsx`, `src/lib/mindmapValidation.js` (신규)

- Dagre 레이아웃 방향: Top-Bottom → **Left-Right** 전환
- `mindmapValidation.js` 도입:
  - AI 출력의 순환 구조, 누락 레벨, 중복 ID 자동 정규화
  - 유효 그룹(`root, core, process, structure, effect, example, application`) 외 값 자동 보정
  - 구조적 품질 경고 생성 (순환, 고립 노드, 관계 엣지 과다 등)
- 노드 컬러 팔레트를 브랜드 컬러(#070761 남색, #41FFA7 민트) 기반으로 교체
- ReactFlow 워터마크 제거(`proOptions.hideAttribution`)

---

### 7. 문서 라이브러리 UI 전면 재설계

**관련 파일**: `src/pages/LibraryPage.jsx`, `src/pages/LibraryPage.css` (신규)

- **레이아웃**: 상단 헤더 → 좌측 고정 사이드바(408px) + 우측 메인 콘텐츠 CSS 그리드
- 실시간 검색 바 추가 (`doc.name`, `doc.folder` 기준 필터링)
- 폴더 카드에 파일 수 표시 (`${count}개의 파일`)
- 정렬 UI: 버튼 pill 3개 → 단일 `<select>` 드롭다운으로 통합
- 빈 상태(Empty State)에 업로드 CTA 버튼 추가

---

### 8. 툴바 반응형 + 팝업 질문 토글

**관련 파일**: `src/components/Toolbar/TopToolbar.jsx`

- 760px 이하 뷰포트: 컴팩트 단일 컬럼 레이아웃 자동 전환 (높이 48px → 158px)
- "팝업 질문 받기" iOS 스타일 토글 스위치 신규 추가 (`questionGateEnabled` 상태 제어)
- "페이지 / 스크롤" 뷰 모드 세그먼트 컨트롤 추가
- 줌 레벨 부동소수점 오류 수정: `Math.round(... * 10) / 10` 적용

---

### 9. 채팅 맥락 표시 방식 간소화

**관련 파일**: `src/components/Sidebar/ChatPanel.jsx`

- **기존**: 맥락 추가 시 개별 칩 나열 배너가 채팅 상단 점유
- **변경**: 입력창 위 단일 미리보기 영역(`composerContext`) — 제목 + 본문 요약 1줄 + 전체 삭제(×) 버튼만 표시
- 문서 색인 상태 알림 배너 추가 (색인 중: 노란 배경 / 실패: 빨간 배경)
- 소스 칩(`[p.N]`) 우클릭 시 "후속 질문 추가" 다이얼로그 표시
- 입력창 `rows={2}` → `rows={1}` 단일 라인으로 축소

---

### 10. RAG 컨텍스트 파이프라인 체계화

**관련 파일**: `src/lib/ai/contextPipeline.js` (신규), `src/lib/chunking.js` (신규), `src/lib/ai/promptContracts.js` (신규)

- `chunking.js`: 페이지 전체 단위 → **단락 단위 900자 청크** (120자 오버랩) 분할 색인
- `contextPipeline.js`: 의미 검색 + 선택 앵커 + 현재 페이지 3가지 소스를 병합하여 최적 컨텍스트 구성
- `promptContracts.js`: AI 프롬프트에 인용 규칙(`[p.숫자]` 형식만 허용) 및 미확인 내용 면책 문구 강제 주입
- 잘못된 페이지 번호 인용 자동 감지 및 제거로 AI 환각(hallucination) 감소

---

*문서 생성 기준: 2026-06-03*  
*분석 도구: Git diff `2dfa64f..06e628e`, 정적 코드 분석*
